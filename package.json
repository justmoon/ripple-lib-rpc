{
  "name": "ripple-lib-rpc",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "test": "mocha --reporter spec --compilers coffee:coffee-script/register test/rpc_client.coffee",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "superagent": "^0.18.0"
  }
}
